#include <stdio.h>
int main()
{
float len,wid,area;
printf("Enter length:");
scanf("%f",&len);
printf("Enter width:");
scanf("%f",&wid);
area=len*wid;
printf("Area of rectangular=%f",area);
return 0 ;
		
}
