#include <stdio.h>
int main()
{
float len,wid,per;
printf("Enter length:");
scanf("%f",&len);
printf("Enter width:");
scanf("%f",&wid);
per=2*(len+wid);
printf("Permiter of rectangular=%f",per);
return 0;
		
}
